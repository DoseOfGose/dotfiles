[alias]
	lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)'
	lga = !git lg --all
[init]
	defaultBranch = development
# Can use [includeIf] definitions for managing work and personal credentials based directory structure
[user]
	name = Eric Gose
	email = 15129982+DoseOfGose@users.noreply.github.com
[push]
	default = current
[credential]
	helper = osxkeychain
